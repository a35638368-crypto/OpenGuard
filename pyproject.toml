[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "openguard"
version = "0.1.0"
description = "A lightweight open-source server security scanner"
requires-python = ">=3.10"

[project.scripts]
openguard = "openguard.cli:main"
